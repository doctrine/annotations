language: php

sudo: false

cache:
  directory:
    - $HOME/.composer/cache

php:
  - 7
  - 7.1
  - hhvm

matrix:
    allow_failures:
        - php: hhvm
        
before_script:
    - composer install -n
    - if [[ $TRAVIS_PHP_VERSION = hhvm* ]]; then echo hhvm.php7.all=1 >> /etc/hhvm/php.ini; fi
